function ChordBuilder(table) {

    this.build = function() {
        return [
            { octave: 0, note: 0 },
            { octave: 1, note: 1 }
        ];
    } //build

} //ChordBuilder